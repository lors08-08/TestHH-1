const initialState = {
  authorizing: false,
  token: "",
  error: false
};

function logProcedures(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default logProcedures;
