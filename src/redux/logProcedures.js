const initialState = {
  authorizing: false,
  error: false
};

function logProcedures(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default logProcedures;
